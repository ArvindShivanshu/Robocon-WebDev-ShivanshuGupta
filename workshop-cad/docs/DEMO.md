# CAD Project Demo Script & Scope Cuts

---

## 1. Numbered Golden-Path Demo Script (3-Minute Presentation)

1. **Opening Hook (0:00 - 0:30)**:
   - Navigate to the deployed URL.
   - Show the hero section: SolidWorks 3D CAD & Altium ECAD Co-Design Workshop.
   - Point out the live remaining seat counters for both the 3D CAD track and the ECAD track.
2. **Interactive CAD Studio Demonstration (0:30 - 1:30)**:
   - Switch to the interactive 3D / Altium ECAD Canvas.
   - Drag an IC component (`U1 STM32`) and observe connected copper traces stretch in real time.
   - Switch to "Route 45° Net" mode, click pin terminals to route a new copper trace on layer `L1_TOP`.
   - Intentionally drag a component close to another to demonstrate real-time DRC collision hatch warnings.
3. **Attendee Registration & Live Booking (1:30 - 2:15)**:
   - Scroll down to the registration form.
   - Fill in attendee details: name, email, university/company, track choice (`both_mechatronics`).
   - Click "Register & Reserve Seat".
4. **Confirmation & Real-Time Sync (2:15 - 3:00)**:
   - Show the instant confirmation modal displaying unique ticket code (`REG-CAD-XXXX`) with confetti.
   - Observe the live seat counter decrement by 1, demonstrating real-time transactional database consistency.

---

## 2. Scope-Cut Hierarchy
> **Trigger Rule**: If the golden path isn't fully working by hour 3, cut in order, no debate.

1. **Cut 1 (First to drop)**: Interactive 45° trace routing mode in ECAD canvas (fall back to viewing pre-routed traces).
2. **Cut 2**: Real-time DRC collision hatch rendering (fall back to static clearance badge).
3. **Cut 3**: 3D wireframe enclosure rotation controls (fall back to high-res interactive 2D orthographic projection).
4. **Cut 4**: Digital ticket download / QR code export (keep text-based Registration ID modal).
5. **Never Cut (Sacred Core)**: Clean landing page, live remaining seat count, registration form, and successful database reservation.

---

## 3. Mandatory Screen-Capture Backup
At **T+210 minutes** (30 minutes before final freeze), the team must record a complete screen capture (with audio commentary) performing the full golden path demo from start to finish. If live cloud services or network connectivity fail during presentation, immediately switch to the backup video recording.
