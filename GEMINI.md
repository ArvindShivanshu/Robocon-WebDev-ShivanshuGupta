# Project Instructions & Rules

## Automatic Git Push
- After completing changes requested in any prompt, always automatically stage all modified files, commit with a concise descriptive message, and push to GitHub (`git add . && git commit -m "..." && git push`).
